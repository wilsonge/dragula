{
  "name": "dragula",
  "version": "3.7.3",
  "description": "Drag and drop so simple it hurts",
  "source": "src/dragula.js",
  "browser": "dist/dragula.js",
  "browser-min": "dist/dragula-min.js",
  "scripts": {
    "build": "npm run scripts && npm run styles",
    "deploy": "npm run build && npm run deployment && npm run sync",
    "deployment": "git add dist && npm version ${BUMP:-\"patch\"} && git add package.json && git commit -am \"Autogenerated pre-deployment commit\" && git reset HEAD~2 && git add . && git commit -am \"Release $(cat package.json | jq -r .version)\" && git push --tags && npm publish && git push",
    "scripts": "npm run lint && parcel build",
    "start": "watchify -dvo example/example.min.js example/example.js & parcel watch & stylus -w dragula.styl --import node_modules/nib -o dist",
    "serve": "serve",
    "styles": "stylus dragula.styl --import node_modules/nib -o dist && cleancss dist/dragula.css -o dist/dragula.min.css",
    "sync": "git checkout gh-pages ; git merge master ; git push ; git checkout master",
    "lint": "eslint . --format stylish",
    "test": "npm run lint && playwright test",
    "test-legacy": "npm run lint && browserify test-legacy/*.js | tape-run"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bevacqua/dragula.git"
  },
  "author": "Nicolas Bevacqua <nicolasbevacqua@gmail.com> (http://bevacqua.io/)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/bevacqua/dragula/issues"
  },
  "homepage": "https://github.com/bevacqua/dragula",
  "dependencies": {
    "@swc/helpers": "^0.4.14",
    "contra": "1.9.4",
    "crossvent": "1.5.5"
  },
  "devDependencies": {
    "@babel/core": "^7.20.2",
    "@playwright/test": "^1.42.1",
    "browserify": "16.5.2",
    "clean-css": "4.2.3",
    "clean-css-cli": "^4.3.0",
    "eslint": "^8.26.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.26.0",
    "nib": "1.1.2",
    "parcel": "^2.12.0",
    "serve": "^14.1.2",
    "stylus": "0.63.0",
    "tape": "5.7.5",
    "tape-run": "11.0.0",
    "uglify-js": "3.11.0",
    "watchify": "4.0.0"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "targets": {
    "browser": {
      "includeNodeModules": true,
      "optimize": false
    },
    "browser-min": {
      "includeNodeModules": true,
      "optimize": true
    }
  }
}
